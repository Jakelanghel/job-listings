import React from "react";
import { StyledHeader } from "./Header.Styled";

const Header = () => {
  return (
    <StyledHeader>
      <h1>jobs</h1>
    </StyledHeader>
  );
};

export default Header;
